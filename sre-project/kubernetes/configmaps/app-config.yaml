apiVersion: v1
kind: ConfigMap
metadata:
  name: sre-config
  namespace: default
  labels:
    app: sre-microservices
    environment: production
data:
  DB_HOST: "postgres-service"
  DB_NAME: "sre_db"
  DB_USER: "sre_user"
  REDIS_HOST: "redis-service"
  LOG_LEVEL: "INFO"
